---
interface Props {
  class?: string;
  variant?: "light" | "dark";
}

const className = Astro.props.class ?? "";
const variant = Astro.props.variant ?? "dark";
---

<a class:list={["brand-logo", `brand-logo-${variant}`, className]} href="/" aria-label="V12 Electric home">
  <img
    src={variant === "light" ? "/images/v12-electric-logo-alt.png" : "/images/v12-electric-logo-white.png"}
    width={variant === "light" ? 1113 : 1067}
    height={variant === "light" ? 564 : 523}
    alt="V12 Electric"
  />
</a>
