---
import BrandLogo from "./BrandLogo.astro";
import { navItems } from "../data/site";

interface Props {
  active?: string;
  overlay?: boolean;
}

const { active = "", overlay = false } = Astro.props;
---

<header class:list={["site-header", { "site-header-overlay": overlay }]} data-reveal="fade">
  <BrandLogo class="header-logo" variant={overlay ? "dark" : "light"} />

  <nav id="primary-nav" class="primary-nav" aria-label="Primary navigation">
    {
      navItems.map((item) => (
        <a href={item.href} aria-current={active === item.label.toLowerCase() ? "page" : undefined}>
          {item.label}
        </a>
      ))
    }
  </nav>

  <a class="button button-primary header-cta" href="tel:+17605676593">Call or text us</a>

  <button
    class="menu-toggle"
    type="button"
    aria-expanded="false"
    aria-controls="mobile-menu"
    aria-label="Open menu"
  >
    <span></span>
    <span></span>
    <span></span>
  </button>

  <div id="mobile-menu" class="mobile-menu" aria-hidden="true" inert>
    <nav aria-label="Mobile navigation">
      {
        navItems.map((item) => (
          <a href={item.href} aria-current={active === item.label.toLowerCase() ? "page" : undefined}>
            {item.label}
          </a>
        ))
      }
    </nav>
    <a class="button button-primary" href="tel:+17605676593">Call us</a>
  </div>
</header>
